@model List<CarBook.Dto.BannerDtos.ResultBannerDto>

@{
    
    Layout = "~/Views/AdminLayout/Index.cshtml";
   
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="page-title-box d-flex align-items-center justify-content-between">
                    <h4 class="mb-0 font-size-18">Ana Sayfa Banner Alanı Yönetim Paneli</h4>

                    <div class="page-title-right">
                        <ol class="breadcrumb m-0">
                            <li class="breadcrumb-item"><a href="javascript: void(0);">Paneller</a></li>
                            <li class="breadcrumb-item active">Ana Sayfa Banner Alanı Yönetim Paneli</li>
                        </ol>
                    </div>
                                
                </div>
            </div>
        </div>     
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Ana Sayfa Banner Alanı</h4>
                        <p class="card-subtitle mb-4">
                            Ana sayfanın banner alanında bulunan bilgileri düzenlemek için bu panel üzerinden işlemler gerçekleştirebilirsiniz. Tema bütünlüğünün
                            bozulmaması için sadece bir adet veri girişi yapınız. Aksi durumlarda tema bütünlüğü bozulacaktır.
                        </p>
                        <div class="table-responsive">
                            <table class="table mb-0">
                                <thead>
                                <tr>
                                    <th>Başlık</th>
                                    <th>Açıklama</th>
                                    <th>Video URL</th>
                                    <th>Video Açıklaması</th>
                                    <th>İşlemler</th>
                                </tr>
                                </thead>
                                <tbody>
                                @if (Model.Count == 0)
                                {
                                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                                        Burada hiç banner yok. Hadi bir tane eklemek için <strong>"Yeni Banner Girişi"</strong> butonuna bas.
                                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                }
                                else
                                {
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.title</td>
                                            <td>@item.description</td>
                                            <td>@item.videoUrl</td>
                                            <td>@item.videoDescription</td>
                                            <td>
                                                <a href="/Admin/AdminBanner/RemoveBanner/@item.bannerId" class="btn btn-outline-danger me-5">Sil</a>
                                                <a href="/Admin/AdminBanner/UpdateBanner/@item.bannerId" class="btn btn-outline-warning me-5">Güncelle</a>
                                                <a href="/Admin/AdminBanner/BannerDetail/@item.bannerId" class="btn btn-outline-dark me-5">YouTube'da Aç</a>
                                            </td>
                                        </tr>
                                    }
                                }
                               


                                </tbody>
                            </table>
                            <a href="/Admin/AdminBanner/CreateBanner" class="btn btn-outline-primary mt-4">Yeni Banner Girişi</a>
                        </div>

                    </div>
                    <!-- end card-body-->
                </div>
                <!-- end card -->

            </div>





        </div>

    </div> <!-- container-fluid -->
</div>
